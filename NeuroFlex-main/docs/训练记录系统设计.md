# 训练记录系统设计文档

## 概述

本文档详细说明了训练记录系统的数据结构、UI设计和扩展性设计，为后续的排行榜功能和数据库接入提供基础。

## 数据结构

### 训练记录标准格式

```javascript
{
  id: 'uuid',                    // 记录唯一ID
  userId: 'user-uuid',           // 用户ID（预留）
  moduleName: 'schulte',         // 训练模块名称
  difficulty: '5x5',             // 难度描述
  score: 95,                     // 分数 (0-100)
  duration: 12500,               // 训练时长（毫秒）
  accuracy: 0.95,                // 准确率 (0-1)
  completedAt: Date,             // 完成时间
  details: {                     // 详细数据（根据模块不同）
    // 舒尔特方格特有字段
    gridSize: 5,
    mode: 'number',              // 'number' | 'color' | 'reverse'
    averageReactionTime: 450,
    fastestReaction: 280,
    errorCount: 2,
    totalNumbers: 25,
    clickCount: 25,
    
    // 听觉注意特有字段
    snr: 20,                     // 信噪比
    backgroundType: 'white',     // 背景音类型
    digitCount: 6,               // 数字数量
    correctDigits: 5,            // 正确数字数
    correctOrder: 4,             // 顺序正确数
    
    // Stroop训练特有字段
    timeMode: 'limited',         // 时间模式
    totalQuestions: 20,          // 题目总数
    correctCount: 18,            // 正确数量
    wrongCount: 2,               // 错误数量
    timeoutCount: 0,             // 超时数量
    averageReactionTime: 650,
    fastestReaction: 420,
    slowestReaction: 980
  }
}
```

### 模块名称映射

```javascript
const MODULE_CONFIG = {
  schulte: {
    name: '舒尔特方格',
    icon: '🔢',
    category: 'attention'
  },
  stroop: {
    name: 'Stroop训练',
    icon: '🎨',
    category: 'attention'
  },
  sequence: {
    name: '序列工作记忆',
    icon: '🧠',
    category: 'memory'
  },
  audio: {
    name: '听觉选择性注意',
    icon: '👂',
    category: 'attention'
  },
  mirror: {
    name: '双侧神经协调',
    icon: '🤝',
    category: 'coordination'
  },
  categorize: {
    name: '规则导向分类',
    icon: '📊',
    category: 'executive'
  },
  'memory-story': {
    name: '情景联想记忆',
    icon: '📖',
    category: 'memory'
  }
}
```

## UI设计

### 记录卡片组件

```vue
<template>
  <div class="record-card" :class="{ expanded: isExpanded }">
    <!-- 基础信息 -->
    <div class="record-header" @click="toggleExpanded">
      <div class="module-info">
        <span class="module-icon">{{ moduleConfig.icon }}</span>
        <span class="module-name">{{ moduleConfig.name }}</span>
      </div>
      <div class="record-meta">
        <span class="time">{{ formatTime(record.completedAt) }}</span>
        <span class="score" :class="getScoreClass(record.score)">
          {{ record.score }}
        </span>
      </div>
    </div>
    
    <!-- 快速统计 -->
    <div class="quick-stats">
      <div class="stat-item">
        <span class="label">难度</span>
        <span class="value">{{ record.difficulty }}</span>
      </div>
      <div class="stat-item">
        <span class="label">用时</span>
        <span class="value">{{ formatDuration(record.duration) }}</span>
      </div>
      <div class="stat-item">
        <span class="label">准确率</span>
        <span class="value">{{ Math.round(record.accuracy * 100) }}%</span>
      </div>
    </div>
    
    <!-- 详细信息（展开时显示） -->
    <div v-if="isExpanded" class="detailed-stats">
      <component 
        :is="getDetailComponent(record.moduleName)"
        :details="record.details"
      />
    </div>
  </div>
</template>
```

### 分数等级样式

```scss
.score {
  &.excellent { // 90-100分
    background: linear-gradient(135deg, #00ff88, #00d4ff);
    color: white;
    box-shadow: 0 0 20px rgba(0, 255, 136, 0.3);
  }
  
  &.good { // 75-89分
    background: linear-gradient(135deg, #00d4ff, #7b2cbf);
    color: white;
    box-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
  }
  
  &.average { // 60-74分
    background: linear-gradient(135deg, #ffaa00, #ff6b35);
    color: white;
    box-shadow: 0 0 20px rgba(255, 170, 0, 0.3);
  }
  
  &.poor { // 0-59分
    background: linear-gradient(135deg, #ff3366, #ff6b35);
    color: white;
    box-shadow: 0 0 20px rgba(255, 51, 102, 0.3);
  }
}
```

## 扩展性设计

### 1. 模块化详情组件

每个训练模块都有对应的详情组件：

```javascript
// 详情组件映射
const DETAIL_COMPONENTS = {
  schulte: 'SchulteRecordDetail',
  stroop: 'StroopRecordDetail',
  sequence: 'SequenceRecordDetail',
  audio: 'AudioRecordDetail',
  mirror: 'MirrorRecordDetail',
  categorize: 'CategorizeRecordDetail',
  'memory-story': 'MemoryStoryRecordDetail'
}
```

### 2. 统计数据聚合

```javascript
// 统计计算函数
export function calculateStats(records) {
  return {
    totalSessions: records.length,
    averageScore: records.reduce((sum, r) => sum + r.score, 0) / records.length,
    averageAccuracy: records.reduce((sum, r) => sum + r.accuracy, 0) / records.length,
    totalTime: records.reduce((sum, r) => sum + r.duration, 0),
    bestScore: Math.max(...records.map(r => r.score)),
    recentTrend: calculateTrend(records.slice(-10)),
    moduleBreakdown: groupByModule(records)
  }
}
```

### 3. 数据库接入准备

```javascript
// 数据同步接口
export class TrainingRecordSync {
  async syncToServer(records) {
    // 批量上传本地记录到服务器
  }
  
  async syncFromServer(userId) {
    // 从服务器获取用户记录
  }
  
  async resolveConflicts(localRecords, serverRecords) {
    // 解决数据冲突
  }
}
```

### 4. 排行榜数据结构

```javascript
// 排行榜条目
{
  userId: 'user-uuid',
  nickname: 'username',
  avatar: 'avatar-url',
  stats: {
    totalScore: 8500,
    averageScore: 85,
    totalSessions: 100,
    bestScores: {
      schulte: 98,
      stroop: 92,
      // ...
    },
    achievements: ['speed_master', 'accuracy_king'],
    lastActive: Date
  }
}
```

## 性能优化

### 1. 虚拟滚动

对于大量记录，使用虚拟滚动：

```vue
<template>
  <VirtualList
    :items="records"
    :item-height="120"
    :buffer="5"
  >
    <template #default="{ item }">
      <RecordCard :record="item" />
    </template>
  </VirtualList>
</template>
```

### 2. 数据分页

```javascript
// 分页加载
export function usePaginatedRecords(pageSize = 20) {
  const currentPage = ref(1)
  const records = ref([])
  
  const loadMore = async () => {
    const newRecords = await loadRecordsPage(currentPage.value, pageSize)
    records.value.push(...newRecords)
    currentPage.value++
  }
  
  return { records, loadMore }
}
```

### 3. 缓存策略

```javascript
// 记录缓存
const recordCache = new Map()

export function getCachedRecord(id) {
  return recordCache.get(id)
}

export function setCachedRecord(id, record) {
  recordCache.set(id, record)
  // 限制缓存大小
  if (recordCache.size > 1000) {
    const firstKey = recordCache.keys().next().value
    recordCache.delete(firstKey)
  }
}
```

## 数据分析功能

### 1. 趋势分析

```javascript
export function analyzeTrend(records) {
  const sortedRecords = records.sort((a, b) => 
    new Date(a.completedAt) - new Date(b.completedAt)
  )
  
  return {
    scoreImprovement: calculateImprovement(sortedRecords, 'score'),
    accuracyTrend: calculateTrend(sortedRecords, 'accuracy'),
    consistencyIndex: calculateConsistency(sortedRecords),
    learningCurve: generateLearningCurve(sortedRecords)
  }
}
```

### 2. 个性化建议

```javascript
export function generateRecommendations(userStats) {
  const recommendations = []
  
  if (userStats.averageAccuracy < 0.8) {
    recommendations.push({
      type: 'accuracy',
      message: '建议降低难度，专注提高准确率',
      priority: 'high'
    })
  }
  
  if (userStats.recentSessions < 3) {
    recommendations.push({
      type: 'frequency',
      message: '保持训练频率，建议每天至少训练一次',
      priority: 'medium'
    })
  }
  
  return recommendations
}
```

## 总结

这个训练记录系统设计具有以下特点：

1. **数据结构标准化** - 统一的记录格式，便于扩展
2. **UI组件模块化** - 可复用的组件设计
3. **性能优化** - 虚拟滚动、分页、缓存
4. **扩展性强** - 支持新模块、新功能
5. **数据分析** - 趋势分析、个性化建议
6. **数据库就绪** - 为后续云端同步做好准备

这为NeuroFlex的训练记录功能提供了坚实的技术基础。